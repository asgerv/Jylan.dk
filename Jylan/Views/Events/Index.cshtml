@model IEnumerable<Jylan.Models.Event>

<div class="container body-content">
    <h2>Administrator Panel</h2>
    <h4>Opret, slet eller rediger events. Tryk 'Rediger' for at se tilmeldinger til eventet.</h4>
    <div>@Html.ActionLink("Opret ny begivenhed", "Create")</div>
    <div class="table-responsive">
        <table class="table table-bordered table-striped">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.StartDateTime)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.EndDateTime)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Price)
                </th>
                <th>
                    Kontrol
                </th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.StartDateTime)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.EndDateTime)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Price)
                    </td>
                    <td>
                        @Html.ActionLink("Rediger", "Edit", new { id = item.EventId }) |
                        @Html.ActionLink("Slet", "Delete", new { id = item.EventId })
                    </td>
                </tr>
            }
        </table>
    </div>
</div>
